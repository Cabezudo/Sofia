
{
  "application": {
    "container": {
      "width": {
        "default": "#{theme.application.container.width.default}",
        "480": "#{theme.application.container.width.480}",
        "750": "#{theme.application.container.width.750}",
        "1024": "#{theme.application.container.width.1024}",
        "1250": "#{theme.application.container.width.1250}",
        "1800": "#{theme.application.container.width.1800}"
      },
      "padding": {
        "top": {
          "default": "10px"
        },
        "right": {
          "default": "#{theme.application.container.padding.right.default}"
        },
        "bottom": {
          "default": "10px"
        },
        "left": {
          "default": "#{theme.application.container.padding.left.default}"
        }
      }
    },
    "title": {
      "font": {
        "font-family": [
          "Titillium Web",
          "sans-serif"
        ]
      }
    },
    "color": {
      "main": "#{theme.application.color.main}"
    }
  },
  "menu": {
    "name": "#{site.name}",
    "description": "the<br>description",
    "font": {
      "size": {
        "default": "#{theme.menu.font.size.default}"
      }
    },
    "items": [
      {
        "name": {
          "es": "item 1"
        },
        "url": "url",
        "logged": null,
        "hide": [
          "url1",
          "url2"
        ],
        "profiles": [
          "profile1"
        ]
      },
      {
        "name": {
          "es": "item 2"
        },
        "url": "url",
        "logged": null
      }
    ]
  }
}
